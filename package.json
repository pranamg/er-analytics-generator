{
  "name": "er-analytics-generator",
  "version": "0.1.0",
  "private": true,
  "description": "Transform ER diagram images into production-ready analytics platforms",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean",
    "parse": "pnpm --filter @er-analytics/er-parser run parse"
  },
  "keywords": [
    "er-diagram",
    "analytics",
    "dashboard",
    "vision-api",
    "schema-parser"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
